// Manus Maps — deprecated. Stub to prevent TS errors.
export async function makeRequest<T>(_endpoint: string, _params?: Record<string, string>): Promise<T> {
  throw new Error("Maps service is not available (Manus dependency removed)");
}
